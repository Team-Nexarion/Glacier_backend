name: Keep Backend Alive

on:
  schedule:
    
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping backend health endpoint
        run: |
          curl -X GET https://glacier-backend-4r0g.onrender.com/ || exit 0
